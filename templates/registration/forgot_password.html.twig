<!DOCTYPE html>
<html lang="en-GB">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@300;400;600;700;800;900&display=swap"
      rel="stylesheet" />
    <link rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/animate.css/4.1.1/animate.min.css" />
    <link rel="stylesheet" href="{{ asset('assets/css/styles2.css') }}" />
    <title>
      Tuniscape
    </title>
  </head>
  <body>
    <section class="login--page">
      <div class="login--container">
        <div class="login--img">
          <img src="{{ asset('assets/img/login1.jpg') }}" alt="" />
        </div>
        <div class="form-container">
          <p class="title">
            Reset Password
          </p>

          {% block body %}
            <p>
              Enter your email address to receive a password reset link.
            </p>

            <div class="login-box">
              {{ form_start(forgotPasswordForm) }}
              <div class="user-box">
                {{ form_widget(forgotPasswordForm.email) }}
                <label for="{{ forgotPasswordForm.email.vars.id }}">
                  Email
                </label>
              </div>
              {{ form_end(forgotPasswordForm) }}
            </div>

            <p>
              Don't have an account?
              <a href="{{ path('app_register') }}" class="a2">Sign up!</a>
            </p>
          {% endblock %}
        </div>
      </div>
    </section>

    <script src="https://kit.fontawesome.com/f0a0962bc1.js"
      crossorigin="anonymous"></script>
  </body>
</html>
