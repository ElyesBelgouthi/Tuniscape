<!DOCTYPE html>
<html lang="en-GB">
<head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
            href="https://fonts.googleapis.com/css2?family=Montserrat:wght@300;400;600;700;800;900&display=swap"
            rel="stylesheet"
    />
    <link
            rel="stylesheet"
            href="https://cdnjs.cloudflare.com/ajax/libs/animate.css/4.1.1/animate.min.css"
    />
    <link rel="stylesheet" href="{{ asset("assets/css/styles2.css") }}" />
    <title>Tuniscape</title>
</head>
<body>
<section class="login--page">

    <div class="login--container" >

        <div class="login--img">
            <img src="{{ asset('assets/img/login1.jpg') }}" alt="" />
        </div>
        <div class="form-container">
            <p class="title">Reset Password</p>


            {% block body %}

            <p>Enter a new password for your account.</p>

            <div class="login-box">
                {{ form_start(resetPasswordForm, {'attr': {'id': 'registration-form'}})  }}
                <div class="user-box">
                    {{ form_widget(resetPasswordForm.plainPassword.first) }}
                    <label for="{{ resetPasswordForm.plainPassword.first.vars.id }}">New Password</label>
                </div>
                <div class="user-box">
                    {{ form_widget(resetPasswordForm.plainPassword.second) }}
                    <label for="{{ resetPasswordForm.plainPassword.second.vars.id }}">Confirm Password</label>
                </div>
                <a href="#" id="submit-form">
                    <span></span>
                    <span></span>
                    <span></span>
                    <span></span>
                    Reset
                </a>
                {{ form_end(resetPasswordForm) }}
            </div>

        </div>

        {% endblock %}
    </div>
</section>
<script src="{{ asset("assets/js/register.js") }}"></script>
</body>
</html>